# Sample Project

Always run the test suite before committing changes.

## Frontend (`src/web/app/`)

This section only applies when editing files under `src/web/app/`.
Always run the test suite before committing changes.
Use the design system components from `src/web/app/components/`.

## Backend

General backend conventions go here, no specific directory mentioned.
Always run the test suite before committing changes.
